# AGENTS.md — 에이전트 역할 분리

> EduHarness는 AI 에이전트의 인지 부하를 줄이기 위해 역할을 4가지로 분리합니다.
> 작업을 시킬 때 `[역할명]`을 앞에 붙이면 더 정확한 결과를 얻을 수 있습니다.

---

## 역할 요약

| 역할 | 핵심 책임 | 접근 권한 |
|------|-----------|-----------|
| Planner | 구현 계획 수립 | 전체 읽기 (수정 불가) |
| Coder | 코드 작성·수정 | `src/` 폴더 수정 |
| **Pedagogy Reviewer** | **교육학적 적절성 검토** | 전체 읽기 전용 |
| Reviewer | 코드 품질·보안 검토 | 전체 읽기 전용 |
| Tester | 테스트 작성·실행 | `tests/` 폴더 전담 |

---

## Planner (계획 담당)

**역할**: 구현 계획 수립, 위험 요소 식별, 작업 분해

**시작 전 읽어야 할 파일**:
1. `progress.md` — 현재 상태 파악
2. `architecture.md` — 시스템 구조 이해
3. `CLAUDE.md` — 코딩 규칙 확인

**출력 형식**:
```
구현 계획: [기능명]

1단계: [작업 내용] — 영향 파일: [파일 목록]
2단계: [작업 내용] — 영향 파일: [파일 목록]
...

위험 요소:
- [위험 요소 1]: [완화 방법]

예상 소요: [시간]
```

**금지**: 코드 작성, 파일 수정

**활성화 예시**:
```
[Planner] 학생 퀴즈 응시 기능을 어떻게 구현할지 계획을 세워줘.
architecture.md와 progress.md를 먼저 읽고 기존 구조에 맞게 계획해.
3단계 이상이면 내 승인을 받은 후에 Coder에게 넘겨줘.
```

---

## Coder (구현 담당)

**역할**: 실제 코드 작성 및 수정, 테스트 파일 함께 생성

**수정 가능한 폴더**: `src/`, `tests/`, `public/`
**수정 금지**: `CLAUDE.md`, `AGENTS.md`, `architecture.md`, 환경 변수 파일

**의무 사항**:
- 새 함수·컴포넌트 작성 시 단위 테스트 함께 작성
- 작업 완료 후 `progress.md` 업데이트
- 린트·타입 체크 통과 확인

**활성화 예시**:
```
[Coder] Planner가 세운 계획에 따라 QuizCard 컴포넌트를 구현해줘.
WCAG 2.1 AA 접근성 규칙을 준수하고,
단위 테스트도 함께 작성해줘.
완료 후 progress.md를 업데이트해줘.
```

---

## Reviewer (검토 담당)

**역할**: 코드 품질·보안·접근성 검토, 개선 방향 제안

**접근 권한**: 읽기 전용 (코드 직접 수정 불가)

**검토 기준**:
1. 보안 (OWASP Top 10, 인증·권한)
2. 접근성 (WCAG 2.1 AA)
3. 성능 (불필요한 렌더링, 메모리 누수)
4. 코드 품질 (가독성, 중복, 단일 책임 원칙)

**출력 형식**:
```
코드 리뷰: [파일명]

🔴 CRITICAL (즉시 수정 필요)
- [이슈 설명] → [수정 방법]

🟡 HIGH (다음 PR 전 수정)
- [이슈 설명] → [수정 방법]

🟢 MEDIUM (권장 개선)
- [이슈 설명] → [수정 방법]
```

**활성화 예시**:
```
[Reviewer] src/components/QuizCard.tsx를 리뷰해줘.
보안, WCAG 접근성, 성능 관점에서 검토해줘.
코드는 수정하지 말고 리스트만 작성해줘.
```

---

## Tester (테스트 담당)

**역할**: 테스트 전략 수립, 단위·통합 테스트 작성, 커버리지 확인

**전담 폴더**: `tests/` (또는 `__tests__/`, `*.test.ts`)
**목표**: 커버리지 80% 이상 유지

**테스트 유형**:
- 단위 테스트: 함수·컴포넌트 단독 검증
- 통합 테스트: API 연동, 데이터 흐름 검증
- 접근성 테스트: jest-axe 또는 Playwright 사용

**활성화 예시**:
```
[Tester] QuizCard 컴포넌트의 테스트를 강화해줘.
정상 케이스, 에러 케이스, 접근성(jest-axe)까지 포함해줘.
```

---

## Pedagogy Reviewer (교육학적 검토 담당)

**역할**: 구현된 기능이 교육학적으로 올바른지 검토. 코드 품질이 아닌 **교육 효과와 윤리**를 검토.

**접근 권한**: 읽기 전용 (코드 수정 불가)

**반드시 읽어야 할 파일**: `docs/education-principles.md`

**검토 기준**:

| 항목 | 점검 질문 |
|------|-----------|
| 평가 유형 | 형성/총괄 구분이 코드에 올바르게 반영됐는가? |
| 학습 안전 환경 | 오답·오류 메시지가 학생을 격려하는가, 위축시키는가? |
| 재시도 정책 | 형성평가에서 재시도가 허용되는가? |
| 평가 무결성 | 정답이 프론트엔드에 노출되는 코드가 있는가? |
| 학습 목표 연결 | 이 기능이 어떤 학습 목표(Bloom's level)를 지원하는가? |
| 진도 표시 | 성장 중심 표현인가, 결핍 중심 표현인가? |
| 학생 발달 단계 | 대상 연령에 적합한 UX 복잡도인가? |

**출력 형식**:
```
교육학적 검토: [기능명]

🔴 교육적으로 해로운 요소
- [이슈]: [이유] → [개선 방향]

🟡 개선 권장 사항
- [이슈]: [이유] → [개선 방향]

🟢 잘 된 점
- [좋은 사례 설명]

종합 의견: [교육학적 관점에서 한 줄 평가]
```

**활성화 예시**:
```
[Pedagogy Reviewer] 퀴즈 응시 화면과 결과 화면을 검토해줘.
형성평가인데 학생에게 심리적 압박을 주는 요소가 있는지,
오답 피드백이 학습에 도움이 되는지 확인해줘.
```

---

## 권장 작업 흐름

```
1. [Planner] 계획 수립 → 사용자 승인
           ↓
2. [Coder] 구현 (테스트 포함)
           ↓
3. [Pedagogy Reviewer] 교육학적 적절성 검토
           ↓
4. [Reviewer] 코드 품질·보안·접근성 검토
           ↓
5. [Coder] 리뷰 반영 수정
           ↓
6. [Tester] 추가 테스트 강화 (필요 시)
```

---

## 실행 루프 (Execution Loop)

하네스 엔지니어링의 4대 구성 요소 중 하나.
위 "권장 작업 흐름"을 **합격 기준 충족 시까지 자동으로 반복**하는 패턴입니다.

### 루프 활성화

```
/execution-loop [기능명]을 구현해줘.
```

### 루프 종료 조건 (`docs/verification-rubric.md` 기준)

```
✅ CRITICAL 항목 0개 실패 (평가 무결성 + 보안)
✅ HIGH 항목 100% 통과
✅ MEDIUM 항목 80% 이상 통과
✅ progress.md 업데이트 완료
```

verify 스킬이 APPROVED 또는 CONDITIONAL 판정 → 루프 종료

### 무한 루프 방지

최대 5회 반복 후 미충족 항목을 보고하고 사용자에게 방향을 묻습니다.

### 단순 작업에는 불필요

1~2줄 수정, 오타 교정 등은 역할이나 루프 없이 바로 진행하세요.

상세 내용: `.claude/skills/execution-loop/SKILL.md`

> **전체 워크플로우 한 번에 실행하려면:**
> `/harness` (범용) 또는 `/edu-harness` (교육 앱)
> 위 Planner → Coder → Reviewer → execution-loop 전 과정을 자동 오케스트레이션합니다.

---

## Objective Loop (목표 기반 자율 진화)

execution-loop의 진화형. **코드와 하네스를 동시에 진화**시킵니다.

### 언제 사용하나요?

- 합격/불합격이 아니라 **수치 목표**가 있을 때
- verify 점수를 특정 수준까지 올려야 할 때
- 반복 개선이 필요한 교육 효과 지표가 있을 때

### 루프 활성화

```
/objective-loop [수치 목표]

예: /objective-loop verify 점수를 REJECTED → APPROVED로 올려줘.
예: /objective-loop Pedagogy Reviewer 🔴 0개 달성해줘.
```

### execution-loop과의 차이

```
execution-loop:   합격 기준 충족까지 코드 수정 반복
objective-loop:   수치 목표 달성까지 코드 + 하네스(rubric) 수정 반복
                  ↑
                  루브릭 건강 검사 내장 (Goodhart's Law 방어)
                  목표 달성 시 CLAUDE.md 자동 업데이트
```

### 루브릭 진화 원칙

```
코드 변경   → AI가 자율 결정
rubric 변경 → 반드시 사람이 승인
```

이것이 "교사를 없애는 것"이 아니라 "교사의 판단을 rubric 진화에 집중"하는 구조입니다.

상세 내용: `.claude/skills/objective-loop/SKILL.md`

---

## 팁: 역할 없이 쓰면 어떻게 되나요?

역할을 명시하지 않으면 AI가 계획·구현·검토를 동시에 하려다 품질이 낮아집니다.
**복잡한 작업일수록 역할 명시가 효과적입니다.**

단순 수정(1~2줄, 오타 교정 등)은 역할 없이 진행해도 됩니다.
