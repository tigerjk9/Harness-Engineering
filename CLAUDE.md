# CLAUDE.md — EduHarness Foundation

> AI 에이전트는 모든 작업을 시작하기 전에 이 파일을 읽어야 합니다.
> 마지막 업데이트: 2026-03-29
>
> 계층 안내:
> - 이 파일 = **애플리케이션 하네스** (이 프로젝트 전용 규칙)
> - 공통 원칙 = `.claude/skills/` (레포지토리 하네스 — 수정하지 말 것)

---

## 프로젝트 목적

**[YOUR_PROJECT_NAME]**은 교육자를 위한 웹 애플리케이션입니다.
- 대상 사용자: [예: 초등학교 교사, 대학생, 온라인 강사]
- 핵심 기능: [예: 퀴즈 생성, 학습 진도 추적, 수업 자료 관리]
- 배포 환경: [예: Vercel, AWS, 로컬 서버]

---

## 작업 시작 전 필수 절차 (반드시 지킬 것)

1. `progress.md`를 읽고 현재 진행 상태를 파악하라
2. `architecture.md`를 읽고 시스템 구조를 이해하라
3. **3개 이상의 파일에 영향을 미치는 작업**이면 계획을 먼저 제시하고 사용자 승인을 구하라
4. 논리적 충돌이 발생하면 즉시 작업을 멈추고 사용자에게 보고하라

---

## 코딩 규칙

### 언어 및 타입
- TypeScript 필수 — `any` 타입 사용 금지
- 함수형 컴포넌트 사용 (React 사용 시)
- **불변성 원칙**: 객체·배열을 직접 수정하지 말 것

```typescript
// ❌ 금지: 직접 수정
user.name = "새 이름"

// ✅ 올바름: 새 객체 생성
const updatedUser = { ...user, name: "새 이름" }
```

### 파일·함수 크기
- 파일: 800줄 이하
- 함수: 50줄 이하, 중첩 4단계 이하
- 초과 시 분리

### 오류 처리
- 모든 async 함수에 try-catch 적용
- 사용자에게 친화적인 오류 메시지 제공
- 민감한 정보는 오류 메시지에 포함하지 말 것

### 비밀 정보
- API 키, 비밀번호, 토큰은 절대 코드에 하드코딩 금지
- 환경 변수(`.env`)만 사용

```typescript
// ❌ 금지
const apiKey = "sk-proj-1234"

// ✅ 올바름
const apiKey = process.env.API_KEY
if (!apiKey) throw new Error("API_KEY 환경 변수가 설정되지 않았습니다")
```

### 접근성 (WCAG 2.1 AA — 필수)
- 모든 이미지에 의미 있는 `alt` 텍스트
- 버튼·링크에 명확한 `aria-label` 또는 텍스트 콘텐츠
- 색상만으로 정보를 전달하지 말 것
- 키보드로 모든 기능 이용 가능해야 함
- 색상 대비: 일반 텍스트 4.5:1 이상, 큰 텍스트 3:1 이상

---

> ⚠️ **교육 앱이 아니라면 아래 섹션(교육 도메인 특수 규칙) 전체를 삭제하세요.**
> 교육 관련 없는 프로젝트에서는 이 규칙들이 불필요한 혼선을 줄 수 있습니다.

---

## 교육 도메인 특수 규칙 (일반 웹앱과 다른 핵심 규칙)

### 규칙 1 — 형성평가 vs 총괄평가 반드시 구분

교육에서 가장 중요한 구분. 혼동하면 학습 흐름 전체가 무너짐.

| 구분 | 형성평가 Formative | 총괄평가 Summative |
|------|-------------------|-------------------|
| 목적 | 학습 과정 피드백 | 학습 결과 측정 |
| 재시도 | **허용 (권장)** | 불가 |
| 즉각 피드백 | **필수** | 제출 후 일괄 공개 |
| 성적부 반영 | 낮은 가중치 또는 미반영 | 정식 반영 |
| 정답 노출 | 즉시 또는 재시도 후 | 교사 설정에 따름 |
| 시간 제한 | 없거나 느슨함 | 엄격 |

- `quiz.type: "FORMATIVE" | "SUMMATIVE"` 필드를 반드시 DB에 포함
- UI와 API 로직 모두 이 구분에 따라 다르게 동작해야 함
- 형성평가에 총괄평가 로직(재시도 금지 등)을 적용하는 것은 CRITICAL 오류

### 규칙 2 — 학습 안전 환경 (Psychological Safety)

학생은 틀릴 권리가 있습니다. 코드가 이를 방해하면 안 됩니다.

```
❌ 금지 메시지                ✅ 권장 메시지
"틀렸습니다."           →    "아직 아니에요. 다시 생각해볼까요?"
"오답입니다. 0점."      →    "이번엔 맞지 않았어요. 힌트를 볼까요?"
"제한 시간 초과."       →    "시간이 다 됐어요. 다음엔 더 잘 할 수 있을 거예요."
"학습 완료율 30%"       →    "벌써 3개 레슨을 마쳤어요!"  ← 성장 중심 표현
```

- 오류 메시지 텍스트를 설계할 때 Pedagogy Reviewer 역할에게 검토 요청 필수
- 진도 표시: 결핍(못 한 것) 기준 ❌ → 성장(한 것) 기준 ✅
- 형성평가 재시도 버튼: **항상** 제공

### 규칙 3 — 평가 무결성 (Assessment Integrity)

총괄평가에 적용. 이를 위반하면 교육 시스템 신뢰도가 무너짐.

- **정답 데이터는 프론트엔드 페이로드에 절대 포함 금지**
  - API 응답에서 `isCorrect`, `correctAnswerId` 등을 포함하는 코드 작성 금지
  - 채점은 서버에서만 수행
- 문항 순서: 학생마다 시드(seed) 기반 랜덤화 — 재현 가능해야 함
- 성적 변경: `GradeAuditLog` 테이블에 반드시 기록 (누가, 언제, 왜 변경했는지)
- 응시 중 이탈: 자동 저장 → 재진입 시 이어서 진행 가능하게

### 규칙 4 — 교육 콘텐츠 버저닝

교육 콘텐츠는 매 학기 수정됩니다. 기존 응시 이력을 깨뜨리지 않으려면:

- 이미 응시된 문항(Question)을 **직접 수정하지 말 것**
  - 대신: 새 버전 생성 후 신규 Quiz에 연결
  - 기존 Progress/Score는 응시 당시 버전으로 영구 보존
- Lesson 내용 수정은 버전 관리 또는 변경 이력 기록

### 규칙 5 — 미성년자 데이터 특별 규정

- 만 14세 미만 사용자: 회원가입 시 보호자 동의 플로우 필수
- 수집 데이터 최소화: 학습에 직접 필요한 항목만 (이름, 학교, 학번)
- 전화번호·주소 등 불필요한 개인정보 수집 금지
- 학생 데이터를 제3자에게 제공하는 코드 작성 시 즉시 작업 중단 후 확인 요청

---

## 금지 행동 (반드시 사용자 확인 후 실행)

| 행동 | 이유 |
|------|------|
| `rm -rf`, 파일 영구 삭제 | 복구 불가 |
| 데이터베이스 스키마 변경 | 데이터 손실 위험 |
| 인증·권한 로직 수정 | 보안 취약점 위험 |
| 환경 변수 추가·삭제 | 배포 환경 영향 |
| 외부 API 설정 변경 | 서비스 중단 위험 |

---

## 작업 완료 기준

작업 완료를 선언하기 전에 반드시:

1. **테스트 실행**: 관련 테스트 명령어 실행 → 결과 확인
2. **progress.md 업데이트**: 완료 항목 체크, 다음 할 일 갱신
3. **변경 요약 제시**: 무엇을 왜 변경했는지 한 문장으로

> "아마 될 것 같습니다", "테스트 없이 확인됐습니다" 같은 표현 금지
> 실행 증거(테스트 결과, 빌드 로그)를 반드시 제시할 것

---

## 주요 명령어

```bash
# 개발 서버 실행
[YOUR_DEV_COMMAND]        # 예: npm run dev

# 테스트 실행
[YOUR_TEST_COMMAND]       # 예: npm test

# 린트 검사
[YOUR_LINT_COMMAND]       # 예: npm run lint

# 타입 체크
[YOUR_TYPECHECK_COMMAND]  # 예: npx tsc --noEmit

# 빌드
[YOUR_BUILD_COMMAND]      # 예: npm run build
```

---

## 참고 문서

- `architecture.md` — 시스템 구조와 데이터 흐름
- `progress.md` — 현재 진행 상황과 다음 할 일
- `AGENTS.md` — 에이전트 역할 분리 방법
- `docs/example-walkthrough.md` — 실전 하네스 구축 워크스루 (처음이라면 여기부터)
- `docs/education-principles.md` — 형성/총괄 평가, Bloom's taxonomy, UDL 원칙
- `docs/harness-guide.ko.md` — 하네스 상세 사용 가이드
- `docs/wcag-checklist.md` — WCAG 접근성 체크리스트
- `.claude/skills/README.md` — 스킬 카탈로그 (harness / edu-harness / 내부 스킬)
